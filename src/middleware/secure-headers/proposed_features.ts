//ff:type feature=middleware type=handler
//ff:what Proposed features

/**
 * These features have been proposed, but the definitions have not yet been integrated into their respective specs.
 */
export type ProposedFeatures =
  | 'clipboardRead'
  | 'clipboardWrite'
  | 'gamepad'
  | 'sharedAutofill'
  | 'speakerSelection'
